Sacnner -------     nextLine(); 
엔터를 만날 때 까지 1줄 읽음
ex) ABC 이후 엔터 치면 -> A,B,C (41,42,43) 읽고 엔터에 해당하는 2개의 바이트를 읽는다 -> 0D(CR : Carage Return), 0A


클래스 메소드 => 스태틱 메소드 => 보편적으로 아무곳에서나 인스턴스를 만들지 않고 사용할 때
특정 인스턴스에 있는 메소드 => 인스턴스 메소드

*코드 전부 작성 후 컨트롤+a -> 컨트롤+i  =>줄 맞추기